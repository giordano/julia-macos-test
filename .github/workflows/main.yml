name: tinystan tests

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch: {}

jobs:

  julia:
    runs-on: ubuntu-latest
    container: mathworks/matlab
    steps:
      - name: Run
        run: |
          echo "Hello world"

