name: Matlab Docker image

on:
  push:
    branches:
      - '**'
  pull_request:
  workflow_dispatch: {}

jobs:

  test:
    runs-on: ubuntu-latest
    container: mosegiordano/julia-ipu-demo:latest
    steps:
      - name: Run
        run: |
          echo "Hello world"

